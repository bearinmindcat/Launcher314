package com.bearinmind.launcher314

/**
 * Graveyard for retired features, kept for reference only. Nothing in this file
 * is wired up anywhere — it compiles to an empty object and does nothing at
 * runtime. When a feature is retired, its original code is preserved here as
 * comments together with where it used to hook in, so it can be resurrected or
 * studied later.
 *
 * (Currently empty — the fuzzy app-drawer search that briefly lived here was
 * brought back into active use.)
 */
object LegacyFeatures
